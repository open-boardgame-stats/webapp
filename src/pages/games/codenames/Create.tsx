import React from "react";

import Layout from "../../../components/Layout";
import Form from "./Form";

const CreateCodenamesMatch = () => {
  return (
    <Layout>
      <Form />
    </Layout>
  );
};

export default CreateCodenamesMatch;
